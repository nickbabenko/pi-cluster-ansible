---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: cloudflare-access-grafana
  name: cloudflare-access-grafana
spec:
  ports:
    - name: http
      port: {{.Value.service.port}}
      targetPort: {{.Value.deployment.port}}
  selector:
    app: cloudflare-access-grafana
