---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: cloudflare-access-grafana
  name: cloudflare-access-grafana
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflare-access-grafana
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: cloudflare-access-grafana
    spec:
      containers:
        - image: "{{ .Values.image.name }}:{{ .Values.image.tag }}"
          name: cloudflare-access-grafana
          ports:
            - containerPort: {{.Values.deployment.port}}
          env:
            - name: AUTHDOMAIN
              value: {{.Values.config.auth_domain}}
            - name: POLICYAUD
              value: {{.Values.config.policy_aud}}
            - name: FORWARDHEADER
              value: {{.Values.config.forward_header}}
            - name: FORWARDHOST
              value: {{.Values.config.forward_host}}
            - name: ADDR
              value: {{.Values.config.address}}
      restartPolicy: Always
